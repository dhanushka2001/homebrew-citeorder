# Homebrew Tap for citeorder

This repository contains the [Homebrew](https://brew.sh) formula for installing **citeorder**,  
a simple command-line tool that reorders footnotes in Markdown files.


## Installation

You can install citeorder directly from this tap:

```bash
brew install dhanushka2001/citeorder/citeorder
```

Or, if you prefer to add the tap explicitly first:

```bash
brew tap dhanushka2001/citeorder
brew install citeorder
```

